<%= form_for(@event_accelerometer) do |f| %>
  <% if @event_accelerometer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_accelerometer.errors.count, "error") %> prohibited this event_accelerometer from being saved:</h2>

      <ul>
      <% @event_accelerometer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :wristband_id %><br>
    <%= f.text_field :wristband_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
